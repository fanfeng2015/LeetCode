package decodeWays;

// LeetCode #91

public class DecodeWays {

	// M[i]: number of ways to encode substring from index i to n - 1
	public int numDecodings(String s) {
		int[] M = new int[s.length()];
		for (int i = s.length() - 1; i>= 0; i--) {
			int cur = Character.getNumericValue(s.charAt(i));
			if (cur == 0) {
				M[i] = 0;
			} else if ( i == s.length() - 1) {
				M[i] = 1;
			} else {
				
			}
		}
	}

//	private int numDecodings(char[] charArray, int left, int right) {
//		// base case
//		if (left == right) {
//			return 1;
//		}
//		int val = 10 * Character.getNumericValue(charArray[left]) + Character.getNumericValue(charArray[left + 1]);
//		if (left == right - 1) {
//			return val <= 26 ? 2 : 1;
//		}
//		return val <= 26 ? 1 + numDecodings(charArray, left + 1, right) + 2 + numDecodings(charArray, left + 2, right)
//				: 1 + numDecodings(charArray, left + 1, right);
//	}

}
