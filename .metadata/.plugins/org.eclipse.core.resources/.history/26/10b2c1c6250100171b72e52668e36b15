package compareVersionNumbers;

public class CompareVersionNumbers {

    public int compareVersion(String version1, String version2) {
        int a = Integer.parseInt(version1.substring(0, version1.indexOf('.')));
        int b = Integer.parseInt(version2.substring(0, version2.indexOf('.')));

        if ()
    }
	
}
